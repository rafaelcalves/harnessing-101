# H101-91 — stage-one expectation traceability

2026-09-21. Review target: UI-01..04, dispatched as commit `9d2cce5`. Primary surface: `internal/adaptercontract/expected/spec.go`, lines 1–73. Read scenario consumers only to establish fixture inputs and the meaning of expectations. No adapters inspected, tests run, or pass/fail verdict issued. Line references below refer to the documents at review time.

**Ruling: partially traceable.** The expected state and error vocabulary have specification sources. The exact receipt revision `1` does not. Fixture-selected names and IDs need not occur verbatim in architecture documents: the requirement is to preserve those supplied values. Static initialization establishes that these expectations do not read adapter output at runtime; it cannot establish how their author originally selected them. No finding below asserts reverse-engineering without evidence.

## Per-item ruling

| Item | Expected values and source | Ruling |
| --- | --- | --- |
| UI-01 | `spec.go:42–44`: request `ui01-r1`, agent `engineer`, name `Engineer` are supplied scenario inputs. ADR 0003:41,60 requires preservation; boundaries:19,24 defines receipt identity and registration fields. | Traceable except `Revision: 1`. Boundaries:19 requires a committed revision but does not specify its initial value or increment. |
| UI-02 | `spec.go:69–72`: InvalidArgument, Denied, Conflict, NotFound are explicitly listed at boundaries:13; ADR 0003:42 requires preserving stable codes. Unauthorized acceptance maps to Denied at boundaries:28,30; unknown IDs/messages map to NotFound at boundaries:24,32; changed-request payload and expected-revision mismatch map to Conflict at boundaries:38. | Code vocabulary is traceable. These four definitions contain no IOFailure or uncertain-outcome expectation, so they provide no traceability verdict on that path. The consumer's stale `fromStatus` → Conflict case is not explicitly mapped by these documents: boundaries:24 lists valid transitions, while :38 names revision mismatches, not status-precondition mismatches. That precise mapping is a specification gap. |
| UI-03 | `spec.go:49`: `ui03-r1` is supplied input. Original-receipt replay and changed-payload Conflict follow ADR 0003:43 and boundaries:38. Replaying without a new mutation is also supported by boundaries:77. | Traceable except the original receipt's numeric revision `1`, for the same reason as UI-01. Equality to the original receipt is specified independently of its numeric value. No generated-ID expectation appears here. |
| UI-04 | `spec.go:60–64`: Done follows authorized acceptance (boundaries:28,30; ADR 0003:44). `Investigate` and `engineer` are fixture title/assignee inputs preserved under ADR 0003:41,60 and boundaries:24. Current result `res2` follows the second report selecting a new result and acceptance of that exact result (boundaries:26,28,77). | Traceable state expectations. `AgentCount: 2` follows the fixture's two registrations, complete enumeration (ADR 0003:27,29), and separate host reviewer policy (ADR 0003:19; boundaries:30). It is scenario arithmetic, not a universal two-agent requirement. Reviewer configuration does not itself request registration; the documents separate these roles but do not explicitly state “reviewer setup creates no agent record.” |

Shared constants at `spec.go:12–19` and reviewer selection at :23–24 are fixture choices, permitted by opaque IDs (boundaries:11) and trusted harness principal/policy setup (ADR 0003:19,56). Their spelling is not an independently mandated product value. The four error constants reuse production domain symbols; their current spellings match boundaries:13, but the package does not independently pin those strings against a future change to the symbols.

## Named ambiguities

1. **Generated entity IDs:** boundaries:53 specifies workspace-unique opaque IDs and deterministic test IDs; ADR 0003:43 requires generated IDs to be surfaced for recovery. Neither defines which omitted entity IDs must be generated, their display representation, or how independently generated entity IDs are compared across runs. ADR 0003:60 permits normalization only for generated **event** IDs, not entity IDs. Stage one's explicit `t1`, `m1`, `res1`, `res2` values avoid this gap; they do not settle it.
2. **IOFailure:** ADR 0003:42 clearly requires error preservation, request identity, and honest uncertainty. It does not specify a deterministic fault-injection mechanism. Furthermore, the two requested source documents have not reconciled the later taxonomy: ADR 0004:19–31 specifies OutcomeUncertain for uncertain mutation outcomes and reserves ordinary IOFailure for other failures; :55 states the conservative rule during migration. Thus “post-application failure must literally be IOFailure” cannot be justified from the older row alone. There is a document reconciliation gap and no IOFailure expectation in `spec.go`, not evidence that the author chose an unsupported error literal for this path.
3. **Mailbox revisions:** boundaries:34,57 requires separate delivery facts/events and durable recording, not a fixed total workspace revision after SendMessage or a fixed scheduling order for publication/ingestion. No such total appears in `spec.go`. Its :28–29 blanket claim that **each successful command** increments revision by one from zero has no source in these documents and is too broad: original-receipt replay and acknowledgement no-ops are explicit exceptions (boundaries:32,38). Exact initialization, increments for new commits/no-ops, and mailbox integration scheduling need a stated contract before literal arithmetic is certified. Task revision is explicitly distinct (boundaries:28), but its initial numeric value and increment are likewise unstated; the scenario consumers' numeric task-revision preconditions cannot be sourced from that distinction alone.

No specification, tests, or adapter code changed to close these gaps. This ruling certifies the sources identified above, not complete UI-01..04 coverage or suite execution. UI-05..08 remains outside this stage.
