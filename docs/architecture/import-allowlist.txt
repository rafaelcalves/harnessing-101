# H101-19 Phase 1 import containment
#
# This is the deliberately weaker, checkable rule from H101-27: CI checks
# resolved package imports with `go list`. It does not prove that a package
# avoids method values, wrappers, or CommitRequest construction; the future
# standard-library AST checker must provide that stronger guarantee.
#
# Production packages allowed to import the concrete state store:
github.com/rafaelcalves/harnessing-101/internal/adapters/statestore
github.com/rafaelcalves/harnessing-101/internal/host # sole composition root; constructs FileStore privately; does not expose persistence.

# Production packages allowed to import the ports package, which contains the
# outbound persistence contract:
github.com/rafaelcalves/harnessing-101/internal/adapters/statestore
github.com/rafaelcalves/harnessing-101/internal/core/task
github.com/rafaelcalves/harnessing-101/internal/host # sole composition root; injects persistence privately; does not expose mutation.

# Test-only exception: statestore and its own *_test.go files may exercise
# arbitrary StateStore mutations for adapter contract tests. `go list`'s
# Imports field excludes test imports, so those files do not enter the
# production import check.

# H101-43 presentation role rule:
# production presentation packages must reach commands and queries through
# host.FrontendSession, not by importing host or core/task directly. The
# current CLI is the interim composition adapter: it still imports host (and
# request records from core/task) while the adapter extraction is pending.
# Keep this exception explicit and temporary; new presentation packages must
# not be added here without a replacement session-only assembly boundary.
github.com/rafaelcalves/harnessing-101/cmd/harnessing # interim CLI composition adapter; direct host/task imports are being migrated to FrontendSession.
