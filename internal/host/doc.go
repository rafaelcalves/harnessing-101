// Package host will assemble adapters against the core's ports and own
// workspace locking, adapter construction, and controller lifetime. See
// boundaries.md, "Hosting, locality, and what must not leak". Phase 0:
// layout only, no wiring yet.
package host
